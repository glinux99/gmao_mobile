enum EnumActions { VIEW, UPDATE, DELETE, SAVE }

enum ModelNamesEnum {
  analyseTools,
  analyse,
  dueAnalyses,
  performedAnalyses,
  sample,
  transaction,
  transactionDetails,
  allTransactions,
  partner,
  store,
  user,
  wallet,
  payment,
  transfer,
  currency,
  permission
}

enum MessageType { error, warning, success, info }

enum TransactionTypesEnum { expense, income, increaseStock }

enum ConnectedUser { admin, guichet, gestock, production }
